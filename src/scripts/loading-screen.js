import { gsap, TweenLite } from "gsap";

const LoadingScreen = {
    el: document.querySelector('.loading-screen'),

    testFunction() {
        console.log('test')
    },
};

export default LoadingScreen;
